//  class polygon = {constructor(
//   // Define parameters:
//   active, volume, ggbName, label, points, segments, deleted, area,
//  ){
//   // Define properties:
//   this.active = active; this.volume = volume; this.ggbName = ggbName; this.label = label; this.points = points; this.segments = segments; this.deleted = deleted; this.area = area;
//  }};
